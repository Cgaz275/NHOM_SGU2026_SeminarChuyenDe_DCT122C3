// src/services/aiService.js
const { getToneInstruction } = require('../utils/toneMapper');

// Code gọi LLM API sẽ được AI Agent viết tiếp ở đây...
// Ví dụ mường tượng: const toneInstruction = getToneInstruction(cardData.aiConfig.toneOfVoice);